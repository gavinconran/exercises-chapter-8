[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "chapter-7"
version = "0.1"
dependencies = ["numpy", "pytest", "ipython"]

[tool.hatch.build.targets.wheel]
packages = ["example_code", "groups", "scripts"]

